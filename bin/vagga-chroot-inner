#!/bin/sh -ex

mount -t proc proc $root/proc
mount --rbind /sys $root/sys
mount --rbind /dev $root/dev
mount --rbind . $root/work

#export PATH=/bin:$PATH

cd $root$workdir
exec chroot $root /bin/sh -xc "cd $workdir; exec $@"
