containers:
  v35-calc:
    setup:
    - !Alpine v3.5
    - !Install [bc]

commands:
  vagga: !CapsuleCommand
    description: Recursive vagga
    run: [vagga]

  sh: !CapsuleCommand
    description: Recursive vagga
    run: [sh]
