Redis
-----

Simplest container with redis looks like this:

.. literalinclude:: ../../../examples/redis_alpine/vagga.yaml

Here is more comprehensive example of redis installed on ubuntu and has two
instances started in parallel:

.. literalinclude:: ../../../examples/redis/vagga.yaml
