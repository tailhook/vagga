Elasticsearch
-------------

The elasticsearch example uses :volume:`Persistent` volume so works only on
development version of vagga:

.. literalinclude:: ../../../examples/elasticsearch/vagga.yaml
   :language: yaml

